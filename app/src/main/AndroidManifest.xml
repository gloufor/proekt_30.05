<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bashkirov_opisanie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bashkirov_opisanie">
        <activity
            android:name=".statistika"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/opisanie_layout" />

        <activity
            android:name=".sobstvennye_pppoe"
            android:exported="false" />
        <activity
            android:name=".dhcp"
            android:exported="false" />
        <activity
            android:name=".sobstvennye_statyk"
            android:exported="false" />
        <activity
            android:name=".wi_fi"
            android:exported="false" />
        <activity
            android:name=".opisanie"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>